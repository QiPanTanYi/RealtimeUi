# 表示jedis的服务器主机名
jedis.host=hadoop01

# 表示jedis的服务器端口
jedis.port=6379

# jedis连接池中最大的连接个数
jedis.max.total=60

# jedis连接池中最大的空闲连接个数
jedis.max.idle = 30

# jedis连接池中最小的空闲连接个数
jedis.min.idle=5

# jedis连接池最大的等待连接时间ms值
jedis.max.wait.millis=30000